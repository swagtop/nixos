diff --git a/harmonia-cache/src/config.rs b/harmonia-cache/src/config.rs
index 255040e..954a37f 100644
--- a/harmonia-cache/src/config.rs
+++ b/harmonia-cache/src/config.rs
@@ -137,6 +137,9 @@ pub(crate) struct Config {
     pub(crate) secret_keys: Vec<SecretKey>,
     #[serde(skip)]
     pub(crate) store: Store,
+
+    #[serde(default)]
+    pub(crate) logfile_path: Option<String>,
 }
 
 impl Config {
diff --git a/harmonia-cache/src/extra_log.rs b/harmonia-cache/src/extra_log.rs
new file mode 100644
index 0000000..a35789f
--- /dev/null
+++ b/harmonia-cache/src/extra_log.rs
@@ -0,0 +1,22 @@
+use std::error::Error;
+use std::fs::File;
+use std::io::prelude::*;
+
+use actix_web::{HttpResponse, web};
+
+use crate::config;
+
+pub(crate) async fn get(
+    config: web::Data<config::Config>,
+) -> Result<HttpResponse, Box<dyn Error>> {
+    match &config.logfile_path {
+        Some(path) => {
+            let mut file = File::open(path)?;
+            let mut contents = String::new();
+            file.read_to_string(&mut contents)?;
+
+            Ok(HttpResponse::Ok().body(contents))
+        },
+        None => Err("No extra logfile defined in configuration.".into()),
+    }
+}
diff --git a/harmonia-cache/src/main.rs b/harmonia-cache/src/main.rs
index 22ee8e8..e93f517 100644
--- a/harmonia-cache/src/main.rs
+++ b/harmonia-cache/src/main.rs
@@ -45,6 +45,8 @@ mod tls;
 mod version;
 mod zstd_body;
 
+mod extra_log;
+
 /// Resolve a 32-char base32 store-path hash to its `StorePath`.
 fn nixhash(settings: &web::Data<Config>, hash: &[u8]) -> Result<Option<StorePath>> {
     // Validate the hash shape so garbage input becomes a 4xx, not a db scan.
@@ -199,6 +201,8 @@ async fn inner_main() -> Result<()> {
             .route(&nix_serve_nar_route, web::get().to(nar::get))
             .route("/serve/{hash}{path:.*}", web::get().to(serve::get))
             .route("/serve/{hash}{path:.*}", web::head().to(serve::get))
+            .route("/log", web::get().to(extra_log::get))
+            .route("/log", web::head().to(extra_log::get))
             .route("/log/{drv}", web::get().to(buildlog::get))
             .route("/version", web::get().to(version::get))
             .route("/health", web::get().to(health::get))
diff --git a/harmonia-cache/src/swaglog.rs b/harmonia-cache/src/swaglog.rs
deleted file mode 100644
index e69de29..0000000
